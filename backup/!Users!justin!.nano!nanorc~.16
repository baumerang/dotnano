## CHANGE NANO DEFAULTS ##

## Smart soft line wrapping at blanks, not just edge of screen
set atblanks

## Auto indent a newly created line to the same number
set autoindent

## Create backup file when saving with ~
set backup

## Backup directory in ~/.nano/backup
set backupdir "~/.nano/backup/"

## Use bold instead of annoying reverse video
set boldtext

## Set characters treated as cloisng b rackets when justifying paragraphs.
set brackets ""')>]}"

## Save last hundred search strings and replacement strings
set historylog

## Scroll buffer contents per half-screen vs per-line
## set jumpyscrolling

## Set color for shortcut key combos
set keycolor magenta

## Enable line numbers
set linenumbers

## Vim-style lock-files when editing files
set locking

## Draw a vertical stripe at column; Color set by set stripecolor
set guidestripe 80

## Set the opening and closing brackets that can be found by bracket searches
set matchbrackets "(<[{)>]}"

## Mouse support. Text selected throuh dragging and holding Shift key
set mouse

## When reading a file, insert it into a new buffer by default
set multibuffer

## Don't convert files from DOS/Mac format
set noconvert

## Set Number Color
set numbercolor magenta

## Remember cursor position of files between editing sessions.
set positionlog

## Set characters treated as punctuation when justifying paragraphs.
set punct "!.?"

## Hold status-bar messages for only 1 keystroke instead of 25
set quickblank

## Saft line wrapping
set softwrap

## Set Status Color
set statuscolor brightcyan

## Sepcify color combination for stripecolor fgcolor, bgcolor
set stripecolor brightgreen

## Allow Nano to be suspended (C-z)
set suspend

## Change default tabsize from 8
set tabsize 4

## Use spaces for tabs
set tabstospaces

## Save automatically on exit without an annoying prompt
set tempfile

## Color combination for title bar
set titlecolor brightcyan

## Treat punctuation as part of words
set wordbounds

## CUSTOM KEY REBINDING ##


## Syntax Highlighting (from https://github.com/scopatz/nanorc)
include ~/.nano/syntax/*.nanorc
